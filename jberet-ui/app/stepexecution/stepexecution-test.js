'use strict';

describe('jberetUI.stepexecution module', function() {

  beforeEach(module('jberetUI.stepexecution'));

  describe('stepexecution controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var stepexecutionCtrl = $controller('StepExecutionCtrl');
      expect(stepexecutionCtrl).toBeDefined();
    }));

  });
});
