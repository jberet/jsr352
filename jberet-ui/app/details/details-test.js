'use strict';

describe('jberetUI.details module', function() {

  beforeEach(module('jberetUI.details'));

  describe('details controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var detailsCtrl = $controller('DetailsCtrl');
      expect(detailsCtrl).toBeDefined();
    }));

  });
});
