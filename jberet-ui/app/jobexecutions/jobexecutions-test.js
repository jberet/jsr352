'use strict';

describe('jberetUI.jobexecutions module', function() {

  beforeEach(module('jberetUI.jobexecutions'));

  describe('jobexecutions controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var jobexecutionsCtrl = $controller('JobexecutionsCtrl');
      expect(jobexecutionsCtrl).toBeDefined();
    }));

  });
});
