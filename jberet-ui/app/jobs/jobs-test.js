'use strict';

describe('jberetUI.jobs module', function() {

  beforeEach(module('jberetUI.jobs'));

  describe('jobs controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var jobsCtrl = $controller('JobsCtrl');
      expect(jobsCtrl).toBeDefined();
    }));

  });
});
