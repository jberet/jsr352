'use strict';

describe('jberetUI.jobinstances module', function() {

  beforeEach(module('jberetUI.jobinstances'));

  describe('jobinstances controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var jobinstancesCtrl = $controller('JobInstancesCtrl');
      expect(jobinstancesCtrl).toBeDefined();
    }));

  });
});
